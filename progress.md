Original prompt: посмотри внимательно проект и давай сделаем рефакторинг. Что я ожидаю: это мобильная игра на хтмл, в которой я могу как разработчик строить из блоков сооружения в дебаг режиме и после этого создавать уровни. Подготовь все под расширение игры, а именно новые фичи, уровни отдельными файлами, но подгружаются все из индекс хтмл. И удали все лишнее не относящееся к этому проекту, например screenshot-clone.html, editor.html и прочее, главный файл -это индекс. Сразу учти, что я планирую добавлять новые блоки в игру с разными текстурами, разных форм

Legacy prompt: Сделай для моего HTML5/JavaScript-проекта простой локальный 2D scene builder/editor, который работает прямо в браузере и помогает собирать сцены визуально без ручного редактирования координат.

- Создан модульный редактор: editor.html/editor.css + js/editor.js + js/ui.js.
- Данные сцены вынесены в js/scene-state.js.
- Canvas-рендер и хиттест вынесены в js/renderer.js.
- Режим runtime добавлен: runtime.html/runtime.css + js/runtime.js.
- Конфиг ассетов и defaults: js/resources.js.
- Добавлен пример сцены: scene-example.json.
- Добавлена стартовая страница: index.html.

TODO / next:
- Marquee box-select (рамка выделения) для более удобного multi-select.
- Гизмо resize по осям (не только uniform scale handle).
- Привязка к направляющим (guides) и smart snapping.
- Группы/контейнеры и prefab-идентификаторы в JSON.
- Упрощено добавление ассетов: кнопка `Add Files` в editor.html + file picker.
- При выборе файлов ассеты добавляются в левую панель автоматически.
- Для JSON используется `src` вида `./assets/<имя_файла>`; размеры читаются из файла.
- Пользовательские ассеты сохраняются в localStorage (`scene_builder:custom_assets`).

- 2026-03-18: Сделан standalone 3D-прототип строительной сцены на Three.js без сборки.
- Запуск: просто открыть `index.html` (file://), без сервера.
- Реализация вынесена в `js/prototype/app.js` (Track controller, Block controller, UI controller, Build manager).
- Механика реализована: запуск блока из нижней панели, перелёт на контур, движение по замкнутому пути по `t`, спавн маленьких блоков в фиксированные позиции дома, возврат блока в ячейку, ограничение на 1 активный блок.
- Ограничения реализованы: уменьшение count на каждый вылет, запрет запуска при count=0, деактивация/скрытие при 0, защита от повторного размещения в одну позицию (reserve/commit в BuildManager).
- Автопроверка Playwright (file://) выполнена: ошибок JS нет, после полного цикла `active_block` становится `null`, прогресс строительства меняется.

TODO / next:
- Подкрутить художественные материалы/текстуры (сейчас procedural + flat материалы).
- При желании добавить аудио/частицы при вылете и приземлении блоков.
- 2026-03-18: Для `file://`-режима добавлены встроенные data-url текстуры (`js/prototype/texture-data.js`) чтобы обойти CORS при загрузке локальных image-файлов.
- На блоки наложены текстуры stone/wood/grass; третий слот изменен на `Grass`.
- На нижний мировой plane наложена травяная текстура (tile repeat).
- 2026-03-18: Пересобран blueprint центра в читаемый дом (пол/стены/крыша), всего 39 позиций, чтобы можно было полностью собрать текущим запасом блоков.
- 2026-03-18: Добавлен встроенный debug level-editor: режим ON/OFF, выбор типа блока, выбор слоя Y, постановка блоков по клику в сетку, erase-режим, clear и export JSON.
- 2026-03-18: Ghost-маркеры цели убраны полностью. Цель строительства теперь загружается из JSON уровня (DEFAULT + кнопка `Use As Target` из debug-панели), поэтому в игре собирается именно то, что задано/собрано в debug.
- 2026-03-18: По референс-скриншоту добавлен новый blueprint стартового уровня (`DEFAULT_EDITOR_LEVEL`) на 59 блоков: платформа, опоры/обвязка, лестница, каменная зона и факелы.
- 2026-03-18: Добавлены новые типы блоков и процедурные текстуры в `js/prototype/app.js`: `planks`, `log`, `cobble`, `torch` (плюс расширен инвентарь в `BLOCK_CONFIGS`).
- 2026-03-18: Реализован ghost-режим цели в `BuildManager`: целевая постройка показывается полупрозрачно до сборки и исчезает по мере `commit`.
- 2026-03-18: `Level Debug` обновлен новыми типами блока (`index.html`).
- 2026-03-18: UI нижней панели расширен под 8 слотов (`styles.css`).
- 2026-03-18: Обновлен cache-bust версии `app.js` в `index.html` до `v=20260318-27`.
- Проверка Playwright (`file://.../index.html`) выполнена после очистки `output/web-game`: `errors-0.json` не создан (новых pageerror нет), `state-0.json` показывает `placed_blocks: 59`, `build_progress.total: 59`, новый инвентарь типов присутствует.

TODO / next:
- Сопоставить blueprint 1:1 с ракурсом скрина через интерактивную ручную подгонку координат (сейчас повторение близкое по форме, но не пиксельно-идентичное к референсу).
- При необходимости добавить отдельный тип `stair` с ступенчатой геометрией вместо кубических ступеней.

- 2026-03-20: Проведен рефакторинг в новую структуру `js/game/*` с единым входом через `index.html`.
- Добавлены конфиги блоков: `js/game/data/block-library.js` (поддержка форм `cube/slab/pillar/stairs/door`, проще расширять библиотеку блоков и текстур).
- Уровни вынесены в отдельные файлы и подгружаются из `index.html`: `js/game/data/levels/registry.js`, `level-01-starter.js`, `level-02-forms.js`.
- В `js/game/app.js` добавлена интеграция level registry: выбор уровня, `levelId` в состоянии, загрузка уровня из debug-панели, экспорт текущего уровня в JS-модуль.
- Debug UI обновлен: список блоков теперь строится динамически из библиотеки, добавлены `Level` selector, `Load Level`, `Export Level JS`.
- Добавлена мобильная адаптация debug-панели в `styles.css` (`@media (max-width: 900px)`).
- Удалены лишние файлы, не относящиеся к текущему проекту (`editor.html`, `runtime.html`, `screenshot-clone.html`, `minecraft-builder.html`, старые `js/editor.js`/`js/runtime.js`/и т.д., старый `js/prototype/*`).
- Проверка Playwright выполнена в `output/web-game-refactor`: сгенерированы `shot-0.png`, `shot-1.png`, `state-0.json`, `state-1.json`; ошибок JS в этом прогоне не записано (файл `errors-*.json` не создан).

TODO / next:
- Вынести `BLOCK_LIBRARY` и `LEVEL_CATALOG` редактирование в отдельный developer-facing README (как добавлять новый блок/уровень шаг за шагом).
- Добавить явный UI-переключатель текущего уровня в runtime HUD (не только debug-панель), если понадобится быстрый QA на телефоне.
- При необходимости добавить отдельные UV/материалы для `stairs` и `slab` (сейчас формы работают, но используют общий материал блока).
- 2026-03-23: Добавлен новый блок `water` в библиотеку блоков (`js/game/data/block-library.js` + fallback в `js/game/app.js`).
- В `LevelEditor` добавлена базовая симуляция жидкости: источники воды (поставленные игроком) автоматически текут вниз, а при упоре в опору растекаются по поверхности в 4 стороны с ограничением дальности.
- Вода хранится как тип `water` с метаданными `waterSource/waterLevel`; авто-сгенерированные потоки пересчитываются от источников и не экспортируются в level JSON (экспортирует только источники).
- Материал воды сделан полупрозрачным (`MeshStandardMaterial` с opacity), чтобы визуально отличать её от твёрдых блоков.
- Подключен `editor.update(dt)` в основном `step(dt)` для тиков симуляции воды.
- 2026-03-23: Для доступности в UI вода переведена на отдельную форму `shape: water` (иначе при `shape: cube` её нельзя было выбрать отдельно в Shape-select, т.к. cube-группа уже занята stone).
- Добавлен label формы `water` в `SHAPE_LABELS` и отдельная геометрия `shape === 'water'` (чуть ниже верхняя грань, полупрозрачный объем).
- Обновлены cache-bust query-параметры в `index.html` для `block-library.js` и `app.js`.
- 2026-03-23: Вода переработана под поведение "1x1 источник": при установке не заполняет область мгновенно.
- Исправлен материал воды: блок `water` игнорирует выбранную texture map, поэтому больше не становится камнем.
- Поток воды теперь пошаговый (`simulateWaterStep`): сначала вниз по 1 клетке, боковое растекание только при наличии замкнутых стен/опоры (контейнер), чтобы не было резкого расползания на открытой поверхности.
